import { AbiItem } from 'web3-utils';

import FTA from './FTA.json';
import FTT from './FTT.json';
import FTC from './FTC.json';

export const ABIs = {
  FTA: FTA as AbiItem[],
  FTT: FTT as AbiItem[],
  FTC: FTC as AbiItem[],
};
